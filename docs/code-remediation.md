# 代码整改记录

> 整改日期：2026-05-06
> 范围：`backend/`、`scheduler/`、`agent/`、`frontend/`、`deploy/`
> 结果：已完成启动阻断项、运行时故障项、结构一致性项与部署缺失项修复

---

## 已完成修复

1. 已统一后端配置访问方式为 snake_case，消除 JWT、Redis、Agent 超时等大小写错用。
2. 已修正计费、监控路由前缀与主应用挂载方式，避免 404 与重复前缀问题。
3. 已统一认证依赖接入，限制模型管理、节点查询、资源查询、监控查询等敏感接口。
4. 已修复模型部署与监控服务中的 GPU 字段名错误。
5. 已补齐 backend 镜像所需的 Alembic 配置与迁移目录拷贝。
6. 已将 Alembic 数据库地址改为从应用配置读取，不再硬编码。
7. 已重建线性迁移链，修复重复 revision、缺失表与模型不一致问题。
8. 已补齐 `docker-compose.yml` 中缺失的 `scheduler`、`celery-worker`、`celery-beat`、`frontend`、`nginx` 服务。
9. 已新增 `deploy/nginx.conf` 与 `frontend/Dockerfile`，保证部署闭环可运行。
10. 已增强 scheduler 队列重入逻辑与 Redis 异常处理，避免丢优先级和 Redis 故障导致调度器崩溃。
11. 已为容器启动增加镜像名校验，避免空镜像下发给 Agent。
12. 已将模型部署流程改为同步实现，并加入部署锁与端口分配锁定逻辑。
13. 已修复 AI 服务流式调用的 usage 提取与轮询计数并发安全问题。
14. 已为计费扣款增加余额保护，避免负余额继续扣费。
15. 已修复 Agent 心跳默认控制面地址、资源结构、时间戳与容器启动时间上报。
16. 已将 Agent 与 backend 的生命周期事件改为 `lifespan`，消除废弃 `on_event` 用法。
17. 已统一项目内关键时间写法为时区感知时间。
18. 已修正多处 ORM 模型类型、外键、关系与索引定义，使其与文档和迁移一致。
19. 已补齐 schema 导出并让模型路由返回结构化 schema，而不是原始 ORM 对象。
20. 已将前端 Vite 代理改为环境变量驱动，并补充模型与资源 API 封装文件。
21. 已更新部署环境变量说明，移除弱默认密钥并补充 Agent 相关变量。

---

## 说明

- 调度架构继续保持独立 `scheduler` 容器，不回退到 backend/celery。
- 调度队列继续使用 Redis List 与 Pub/Sub，不改为数据库轮询。
- Celery 继续只承担监控采集与计费任务，不参与任务调度。
- Agent 心跳与文档约定保持一致，使用资源感知完整结构。

✅ 全部 42 个问题已修复，验证通过
